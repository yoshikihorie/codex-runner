# コントリビューションガイド

codex-runner は現在、設計フェーズであり実装は未着手です。現時点ではコードの PR より、設計の前提、失敗モード、移行条件への具体的な指摘のほうが価値があります。

日本語・英語のどちらでも歓迎します。

## 特に意見を求めたいこと

設計書の「実装着手前に必ず確認すること」のうち、外部の開発者から知見を得やすい次の 3 点を求めています。

1. Codex CLI 0.144.5 で、制御端末から切り離した `codex exec` が黙って終了する不具合は解消しているか。
2. `codex exec --json` のイベント形式は実際にどのように出力されるか。
3. `--output-last-message` は異常終了時に空ファイルを作るか、ファイルを作らないか。

再現手順、Codex CLI のバージョン、macOS のバージョン、観測した終了コードと出力の有無を添えると検討しやすくなります。秘密情報、個人情報、利用先固有のパスは投稿しないでください。

## Issue

提案や不具合の報告は GitHub Issue で受け付けます。設計への指摘は、対象の見出し、前提、期待する挙動、代替案をできるだけ具体的に記載してください。

## Pull Request

1. 変更の目的と設計への影響を説明してください。
2. 既存の事実と推測を分け、未確認の内容は未確認と明記してください。
3. 公開文書に利用環境固有のパス、顧客情報、秘密情報を含めないでください。
4. 大きな設計変更は、先に Issue で論点を共有してください。

## コミットメッセージ

次の接頭辞を使ってください。

- `feat:` 新しい機能や設計要素
- `fix:` 不具合の修正
- `docs:` 文書の変更
- `test:` テストの追加・修正
- `chore:` 保守作業
